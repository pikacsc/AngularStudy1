import { MyHidden2Directive } from './my-hidden2.directive';

describe('MyHidden2Directive', () => {
  it('should create an instance', () => {
    const directive = new MyHidden2Directive();
    expect(directive).toBeTruthy();
  });
});
